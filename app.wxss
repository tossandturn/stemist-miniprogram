page {
  background: #f5f6fb;
  color: #18213d;
  font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Microsoft YaHei", sans-serif;
  font-size: 28rpx;
}

view, text, button, input, textarea { box-sizing: border-box; }
button { border-radius: 12rpx; font-size: 28rpx; line-height: 1.35; }
button::after { border: 0; }
button[disabled] { opacity: .55; }

.page {
  min-height: 100vh;
  padding: 28rpx 32rpx calc(64rpx + env(safe-area-inset-bottom));
  background: linear-gradient(180deg, rgba(232, 239, 255, .82) 0, rgba(255, 255, 255, 0) 360rpx), #f5f6fb;
}
.topbar { display: flex; align-items: center; justify-content: space-between; min-height: 76rpx; }
.brand { display: flex; align-items: center; min-width: 0; }
.brand-mark { display: flex; align-items: center; justify-content: center; flex: 0 0 auto; width: 58rpx; height: 58rpx; margin-right: 16rpx; border-radius: 16rpx; color: #fff; background: #7357e8; font-size: 32rpx; font-weight: 800; box-shadow: 0 10rpx 22rpx rgba(115, 87, 232, .24); }
.brand-title { display: block; color: #18213d; font-size: 31rpx; font-weight: 800; line-height: 1.1; }
.brand-subtitle { display: block; margin-top: 6rpx; color: #66708a; font-size: 20rpx; line-height: 1.25; }
.topbar-status { color: #0f8668; font-size: 21rpx; font-weight: 700; }
.topbar-status::before { display: inline-block; width: 12rpx; height: 12rpx; margin-right: 8rpx; border-radius: 50%; background: #20b486; content: ''; }
.topbar-status.needs-auth { color: #7357e8; }
.topbar-status.needs-auth::before { background: #f19a3e; }

.eyebrow { color: #7357e8; font-size: 22rpx; font-weight: 800; letter-spacing: 2rpx; text-transform: uppercase; }
.title { margin-top: 14rpx; color: #18213d; font-size: 54rpx; line-height: 1.12; font-weight: 800; letter-spacing: -.5rpx; }
.title-line { display: block; }
.subtitle { margin-top: 16rpx; color: #66708a; font-size: 27rpx; line-height: 1.58; }
.section-title { margin: 38rpx 0 14rpx; color: #18213d; font-size: 31rpx; font-weight: 800; }
.section-kicker { color: #6d758b; font-size: 21rpx; font-weight: 700; letter-spacing: 1rpx; text-transform: uppercase; }

.card, .panel { margin-top: 18rpx; padding: 28rpx; border: 1rpx solid #e4e7f0; border-radius: 18rpx; background: #fff; box-shadow: 0 12rpx 30rpx rgba(42, 31, 89, .07); }
.card-title { color: #18213d; font-size: 31rpx; font-weight: 800; line-height: 1.25; }
.card-detail { margin-top: 10rpx; color: #66708a; font-size: 24rpx; line-height: 1.5; }
.card-meta { display: block; margin-top: 14rpx; color: #6d758b; font-size: 21rpx; }

.primary, .secondary { width: 100%; min-height: 88rpx; margin-top: 22rpx; padding: 0 24rpx; font-weight: 750; }
.primary { color: #fff; background: linear-gradient(135deg, #8065ec, #6747d8); box-shadow: 0 10rpx 20rpx rgba(103, 71, 216, .18); }
.secondary { color: #4f35b5; border: 1rpx solid #d9d3ff; background: #f0ecff; }
.text-button { min-height: 64rpx; margin-top: 12rpx; padding: 0 10rpx; color: #7357e8; background: transparent; font-size: 24rpx; font-weight: 750; }

.hero-card { position: relative; overflow: hidden; margin-top: 28rpx; padding: 34rpx 30rpx 30rpx; border: 1rpx solid #dcd5ff; border-radius: 24rpx; background: linear-gradient(135deg, #fff 0%, #f4f1ff 100%); }
.hero-card::after { position: absolute; right: -100rpx; top: -130rpx; width: 330rpx; height: 330rpx; border-radius: 50%; background: rgba(115, 87, 232, .1); content: ''; }
.hero-actions { position: relative; z-index: 1; display: flex; flex-wrap: wrap; margin-top: 20rpx; }
.hero-actions .primary, .hero-actions .secondary { width: auto; min-width: 260rpx; margin: 0 14rpx 0 0; }
.hero-actions .secondary { min-width: 190rpx; }
.metric-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); margin-top: 20rpx; }
.metric { min-height: 150rpx; margin: 8rpx 8rpx 0 0; padding: 22rpx; border: 1rpx solid #e4e7f0; border-radius: 16rpx; background: #fff; }
.metric:nth-child(even) { margin-right: 0; }
.metric-label, .metric-detail { display: block; color: #66708a; font-size: 21rpx; }
.metric-value { display: block; margin-top: 10rpx; color: #18213d; font-size: 36rpx; font-weight: 800; }
.metric-detail { margin-top: 6rpx; color: #6d758b; font-size: 20rpx; line-height: 1.35; }
.learning-loop { margin-top: 20rpx; padding: 24rpx; }
.loop-track { display: flex; align-items: flex-start; margin-top: 18rpx; }
.loop-step { position: relative; display: flex; flex: 1; min-width: 0; }
.loop-step:not(:last-child)::after { position: absolute; left: 34rpx; right: 8rpx; top: 18rpx; height: 2rpx; background: #dfe4ef; content: ''; }
.loop-dot { position: relative; z-index: 1; display: flex; align-items: center; justify-content: center; flex: 0 0 auto; width: 38rpx; height: 38rpx; border-radius: 50%; color: #fff; background: #c7ccda; font-size: 19rpx; font-weight: 800; }
.loop-ready .loop-dot, .loop-next .loop-dot { background: #7357e8; }
.loop-copy { min-width: 0; margin-left: 8rpx; padding-right: 10rpx; }
.loop-copy text, .loop-copy .loop-detail { display: block; }
.loop-copy text { color: #18213d; font-size: 21rpx; font-weight: 800; }
.loop-copy .loop-detail { margin-top: 4rpx; color: #6d758b; font-size: 17rpx; line-height: 1.3; }
.recent-activity { margin-top: 18rpx; padding-top: 14rpx; border-top: 1rpx solid #edf0f4; color: #66708a; font-size: 21rpx; }
.quick-grid { display: grid; grid-template-columns: 1fr; }
.quick-card { position: relative; min-height: 220rpx; padding: 26rpx; overflow: hidden; }
.quick-card::before { position: absolute; right: 24rpx; top: 30rpx; width: 62rpx; height: 8rpx; border-radius: 8rpx; background: #d9d3ff; opacity: .9; content: ''; }
.quick-card.stem::before { background: #7357e8; }
.quick-card.listening::before { background: #7657e8; }
.quick-card.reading::before { background: #3a9d85; }
.quick-card.writing::before { background: #f19a3e; }
.quick-card.speaking::before { background: #ed6486; }
.quick-card .card-title { padding-right: 72rpx; }
.quick-card .secondary { width: auto; min-width: 190rpx; min-height: 70rpx; margin-top: 20rpx; }
.accent-stem { border-top: 6rpx solid #7357e8; }
.accent-listening { border-top: 6rpx solid #7657e8; }
.accent-reading { border-top: 6rpx solid #3a9d85; }
.accent-writing { border-top: 6rpx solid #f19a3e; }
.accent-speaking { border-top: 6rpx solid #ed6486; }
.ai-card { display: flex; align-items: flex-start; padding: 24rpx; }
.ai-orb { display: flex; align-items: center; justify-content: center; flex: 0 0 auto; width: 74rpx; height: 74rpx; margin-right: 18rpx; border-radius: 22rpx; color: #fff; background: #7357e8; font-size: 34rpx; }
.ai-orb-mark { position: relative; width: 30rpx; height: 30rpx; border: 4rpx solid currentColor; border-radius: 50%; }
.ai-orb-mark::before, .ai-orb-mark::after { position: absolute; left: 12rpx; top: -8rpx; width: 4rpx; height: 38rpx; border-radius: 4rpx; background: currentColor; content: ''; }
.ai-orb-mark::after { transform: rotate(90deg); }
.ai-copy { min-width: 0; flex: 1; }
.ai-card .secondary { width: auto; min-width: 170rpx; min-height: 70rpx; margin-top: 18rpx; }

.field-label { display: block; margin: 24rpx 0 10rpx; color: #4e5a76; font-size: 24rpx; font-weight: 750; }
textarea, .input { width: 100%; box-sizing: border-box; padding: 22rpx; border: 1rpx solid #dfe4ef; border-radius: 14rpx; color: #18213d; background: #fff; font-size: 28rpx; line-height: 1.55; }
textarea { min-height: 300rpx; }
.input { min-height: 88rpx; }
.char-count { display: block; margin-top: 8rpx; color: #6d758b; font-size: 20rpx; text-align: right; }
.error { margin-top: 18rpx; padding: 18rpx; border-radius: 14rpx; color: #b33b55; background: #fff0f2; font-size: 24rpx; line-height: 1.45; }
.warning { margin-top: 18rpx; padding: 18rpx; border-radius: 14rpx; color: #8a5b18; background: #fff6e8; font-size: 24rpx; line-height: 1.45; }
.result { margin-top: 20rpx; padding: 24rpx; border: 1rpx solid #dceee7; border-radius: 16rpx; color: #18213d; background: #fff; white-space: pre-wrap; font-size: 26rpx; line-height: 1.6; }
.notice { margin-top: 20rpx; padding: 18rpx; border-radius: 14rpx; color: #5d6781; background: #eef0f8; font-size: 22rpx; line-height: 1.5; }
.status-line { display: flex; align-items: center; justify-content: space-between; margin-top: 20rpx; color: #66708a; font-size: 22rpx; }
.status-dot { display: inline-block; width: 12rpx; height: 12rpx; margin-right: 8rpx; border-radius: 50%; background: #20b486; }
.workspace-header { display: flex; align-items: center; justify-content: space-between; min-height: 84rpx; margin-bottom: 12rpx; }
.workspace-header-title { color: #18213d; font-size: 30rpx; font-weight: 800; }
.workspace-header-action { min-width: 72rpx; min-height: 64rpx; padding: 0 14rpx; color: #5b43c7; background: #f0ecff; font-size: 22rpx; }
.workspace-grid { display: block; }
.context-card { margin-top: 12rpx; padding: 22rpx; border-left: 6rpx solid #7357e8; border-radius: 14rpx; background: #fff; }
.context-card-title { color: #18213d; font-size: 26rpx; font-weight: 800; }
.context-card-text { margin-top: 8rpx; color: #66708a; font-size: 23rpx; line-height: 1.5; }
.action-bar { padding-bottom: env(safe-area-inset-bottom); }

.camera-card { text-align: center; }
.camera-icon { display: flex; align-items: center; justify-content: center; width: 136rpx; height: 136rpx; margin: 0 auto 24rpx; border-radius: 44rpx; color: #7357e8; background: #eeebff; font-size: 64rpx; }
.camera-lens { position: relative; width: 58rpx; height: 42rpx; border: 6rpx solid currentColor; border-radius: 12rpx; }
.camera-lens::before { position: absolute; left: 14rpx; top: 6rpx; width: 18rpx; height: 18rpx; border: 4rpx solid currentColor; border-radius: 50%; content: ''; }
.camera-lens::after { position: absolute; left: 8rpx; top: -13rpx; width: 20rpx; height: 10rpx; border: 5rpx solid currentColor; border-bottom: 0; border-radius: 8rpx 8rpx 0 0; content: ''; }
.photo-preview { display: block; width: 100%; height: 460rpx; margin-top: 22rpx; border-radius: 16rpx; background: #e9edf5; }
.crop-stage { position: relative; width: 100%; height: 720rpx; margin-top: 22rpx; overflow: hidden; border-radius: 20rpx; background: #11182b; }
.crop-content { width: 100%; height: 100%; }
.crop-image { width: 100%; height: 100%; }
.crop-box { position: absolute; left: 8%; top: 14%; width: 84%; height: 72%; border: 4rpx solid #fff; border-radius: 12rpx; box-shadow: 0 0 0 9999rpx rgba(0, 0, 0, .45); pointer-events: none; }
.hidden-canvas { position: fixed; left: -9999px; top: -9999px; opacity: 0; }

@media (min-width: 768px) {
  .page { max-width: 1440px; margin: 0 auto; padding: 32px 48px calc(48px + env(safe-area-inset-bottom)); }
  .title { font-size: 48px; letter-spacing: 0; }
  .subtitle { max-width: 760px; font-size: 16px; }
  .card, .panel { padding: 24px; border-radius: 12px; }
  .card-title { font-size: 22px; }
  .card-detail { font-size: 14px; }
  .context-card { padding: 18px; border-radius: 12px; }
  .context-card-title { font-size: 20px; }
  .context-card-text { font-size: 14px; }
  .route-label, .selected-route, .route-pill { font-size: 14px; }
  .route-chip, .task-chip { min-height: 40px; padding: 0 14px; font-size: 14px; }
  .skill-tags text { padding: 6px 12px; font-size: 13px; }
  .crop-hint { font-size: 14px; }
  .primary, .secondary { min-height: 48px; margin-top: 16px; padding: 0 18px; font-size: 16px; }
  .section-title { margin-top: 30px; font-size: 22px; }
  .hero-card { padding: 40px 42px 34px; }
  .metric-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); }
  .metric { min-height: 108px; margin: 8px 8px 0 0; padding: 18px; border-radius: 10px; }
  .metric-label, .metric-detail { font-size: 13px; }
  .metric-value { margin-top: 8px; font-size: 28px; }
  .metric, .metric:nth-child(even) { margin: 8rpx 8rpx 0 0; }
  .metric:last-child { margin-right: 0; }
  .quick-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); margin-right: -12rpx; }
  .quick-card { margin-right: 12rpx; }
  .workspace-grid { display: grid; grid-template-columns: minmax(0, 1.1fr) minmax(360rpx, .9fr); align-items: start; margin: 0 -10rpx; }
  .workspace-grid > .context-card, .workspace-grid > .panel { margin: 12rpx 10rpx 0; }
  .workspace-grid > .panel { position: sticky; top: 18rpx; }
  .photo-preview { height: 420px; }
  .crop-stage { height: 600px; }
  .crop-page .primary, .crop-page .secondary { max-width: 620rpx; margin-left: auto; margin-right: auto; }
}
@media (min-width: 768px) and (orientation: portrait) {
  .page { padding-left: 48rpx; padding-right: 48rpx; }
  .workspace-grid { grid-template-columns: 1fr; }
  .workspace-grid > .panel { position: static; }
}
@media (max-width: 767px) {
  .page { padding-bottom: calc(154rpx + env(safe-area-inset-bottom)); }
  .hero-actions .primary, .hero-actions .secondary { flex: 1 1 auto; min-width: 0; }
  .ai-card { display: block; }
  .ai-orb { margin-bottom: 14rpx; }
  .ai-card .secondary { width: 100%; }
  .crop-stage { height: 650rpx; }
  .loop-track { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .loop-step { margin-bottom: 14rpx; }
  .loop-step:not(:last-child)::after { display: none; }
}

/* Device classification wins over viewport width. This matters for a
   landscape iPhone (wide CSS viewport) and an iPad mini in portrait (narrow
   CSS viewport): neither should silently inherit the other device's chrome. */
.page.device-phone {
  max-width: none;
  margin: 0;
  padding: 28rpx 32rpx calc(154rpx + env(safe-area-inset-bottom));
}
.page.device-phone .title { font-size: 54rpx; letter-spacing: -.5rpx; }
.page.device-phone .subtitle { max-width: none; font-size: 27rpx; }
.page.device-phone .card, .page.device-phone .panel { padding: 28rpx; border-radius: 18rpx; }
.page.device-phone .card-title { font-size: 31rpx; }
.page.device-phone .card-detail { font-size: 24rpx; }
.page.device-phone .primary, .page.device-phone .secondary { min-height: 88rpx; padding: 0 24rpx; font-size: 28rpx; }
.page.device-phone .metric-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
.page.device-phone .quick-grid { grid-template-columns: 1fr; }
.page.device-phone .workspace-grid { display: block; }
.page.device-phone .workspace-grid > .panel { position: static; }
.page.device-phone .photo-preview { height: 460rpx; }
.page.device-phone .crop-stage { height: 650rpx; }
.page.device-phone button { min-height: 88rpx; }

.page.device-tablet {
  max-width: 1440px;
  margin: 0 auto;
  padding: 32px 48px calc(48px + env(safe-area-inset-bottom));
}
.page.device-tablet .title { font-size: 48px; letter-spacing: 0; }
.page.device-tablet .subtitle { max-width: 760px; font-size: 16px; }
.page.device-tablet .card, .page.device-tablet .panel { padding: 24px; border-radius: 12px; }
.page.device-tablet .card-title { font-size: 22px; }
.page.device-tablet .card-detail { font-size: 14px; }
.page.device-tablet .primary, .page.device-tablet .secondary { min-height: 48px; padding: 0 18px; font-size: 16px; }
.page.device-tablet .metric-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); }
.page.device-tablet .quick-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
.page.device-tablet .workspace-grid { display: grid; grid-template-columns: minmax(0, 1.1fr) minmax(360rpx, .9fr); align-items: start; margin: 0 -10rpx; }
.page.device-tablet .workspace-grid > .context-card, .page.device-tablet .workspace-grid > .panel { margin: 12rpx 10rpx 0; }
.page.device-tablet .workspace-grid > .panel { position: sticky; top: 18rpx; }
.page.device-tablet .photo-preview { height: 420px; }
.page.device-tablet .crop-stage { height: 600px; }
.page.device-tablet .crop-page .primary, .page.device-tablet .crop-page .secondary { max-width: 620rpx; margin-left: auto; margin-right: auto; }
.page.device-tablet button { min-height: 44px; }
.page.device-tablet.portrait { padding-left: 48rpx; padding-right: 48rpx; }
.page.device-tablet.portrait .workspace-grid { display: block; }
.page.device-tablet.portrait .workspace-grid > .panel { position: static; }
