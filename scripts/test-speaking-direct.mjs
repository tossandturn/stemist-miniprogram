import assert from 'node:assert/strict'
import {miniRuntime} from './helpers/mini-runtime.mjs'
export function directFixture(){return {protocol:'qwen-direct-session-v1',endpoint:'wss://fixture.cn-beijing.maas.aliyuncs.com/api-ws/v1/realtime?model=qwen3.5-omni-flash-realtime',token:'st-only-a-test-fixture',expiresAt:new Date(Date.now()+60000).toISOString(),inputSampleRate:16000,outputSampleRate:24000,maxSessionSeconds:1200,sessionUpdate:{type:'session.update',session:{input_audio_format:'pcm',output_audio_format:'pcm',input_audio_transcription:{model:'qwen3-asr-flash-realtime'},turn_detection:null,modalities:['text','audio'],instructions:'Examiner test policy'}},responses:Object.fromEntries(['opening','next','assessment'].map(name=>[name,{type:'response.create',response:{modalities:name==='assessment'?['text']:['text','audio'],instructions:name+' examiner test policy'}}]))}}
const calls=[],r=miniRuntime({modules:{'utils/ieltsLearning':{requestIeltsLearning:async(...args)=>{calls.push(args);return directFixture()}}}}),service=r.load('utils/speakingDirect')
await service.issueDirectSession({taskId:'advertising',recovery:true,completedDialogue:[{role:'system',text:'untrusted'},{role:'user',text:'saved answer'}]})
assert.equal(calls[0][0],'/api/speaking/direct-session');assert.equal(calls[0][1].completedDialogue.length,1)
assert.equal(r.storage.size,0,'Direct credential must not be persisted')
await service.issueDirectSession({completedDialogue:Array.from({length:16},()=>({role:'user',text:'测'.repeat(1000)}))})
assert.ok(Buffer.byteLength(JSON.stringify(calls[1][1]),'utf8')<24*1024,'Recovery context must fit the server byte limit even for CJK text')
for(const mutate of [x=>x.endpoint=x.endpoint.replace('cn-beijing','ap-southeast-1'),x=>x.endpoint='wss://evil.test/realtime',x=>x.endpoint+='&token=leak',x=>x.token='sk-long-lived-key',x=>x.expiresAt=new Date(Date.now()-1).toISOString(),x=>x.expiresAt=new Date(Date.now()+3600000).toISOString(),x=>x.inputSampleRate=24000,x=>x.sessionUpdate.session.turn_detection={},x=>delete x.responses.next]){const invalid=directFixture();mutate(invalid);assert.throws(()=>service.validateDirectSession(invalid))}
console.log('Direct speech credential: Beijing-only WSS, ephemeral token, expiry, PCM, template and no-persistence guards passed.')
for(const name of ['opening','next'])for(const modalities of [['text'],['audio'],['text','text'],['text','audio','audio']]){const invalid=directFixture();invalid.responses[name].response.modalities=modalities;assert.throws(()=>service.validateDirectSession(invalid))}
