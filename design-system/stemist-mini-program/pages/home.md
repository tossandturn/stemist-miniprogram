# Stemist Home Override

This page override takes precedence over the generic generated recommendation in `MASTER.md`.

## Existing product authority

Match the production STEM Studio and IELTSist UI: light canvas `#f5f6fb`, text `#18213d`, muted `#6f7892`, line `#e4e7f0`, brand purple `#7357e8`, and white cards with restrained shadows. Do not introduce the generated dark cinema, pink CTA, Baloo, Comic Neue, or glassmorphism direction into the learning product.

## Layout

- Phone: single-column Today dashboard and fixed bottom navigation.
- iPad: wide top navigation, four metrics, two-column skill cards, and no bottom navigation.
- Preserve safe-area padding and keep the primary action visible above the phone bottom navigation.

## Content

Use the production vocabulary `Today`, `Practice`, `AI Coach`, `Account`, `STEM Studio`, and `IELTSist`. Never invent scores when there are no attempts. Show `Ready`, `0`, or an explicit empty state instead.
