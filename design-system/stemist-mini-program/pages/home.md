# Stemist Home Override

This page override takes precedence over the generic generated recommendation in `MASTER.md`.

## Existing product authority

Match the production STEM Studio and IELTSist UI: light canvas `#f5f6fb`, text `#18213d`, accessible muted `#66708a`, line `#e4e7f0`, brand purple `#7357e8`, and white cards with restrained shadows. Do not introduce the generated dark cinema, pink CTA, Baloo, Comic Neue, or glassmorphism direction into the learning product.

## Layout

- Phone: two-column four-entry grid with generous touch targets; iPad: four-entry row with more breathing room. The home shell does not duplicate the secondary navigation.
- Keep the WeChat login action at the upper-right and the persistent AI Coach button at the upper-right; reserve layout space so neither overlaps the brand or title.
- Preserve safe-area padding and keep the four entry cards and AI control reachable in portrait and landscape.

## Content

Use four clear entry labels: `A-Level 学科`, `IELTS`, `竞赛 / 入学考试`, and `Casio 计算器`. Keep `Today`, `Practice`, `AI Coach`, `Progress`, `Account`, `STEM Studio`, and `IELTSist` inside the subsequent workspaces. Never invent scores when there are no attempts; show an explicit empty or unavailable state instead.
