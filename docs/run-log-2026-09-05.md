# Stemist 小程序长任务运行记录 · 2026-09-05

## 本轮接续 · 竞赛真题与运行问题

- 当前目标：竞赛收敛为真题目录，精简学生文案，修复实际用户操作报错。
- 下一步依据：开发者工具原截图证明 AI 遮挡品牌和卡片拥挤；行为回归复现 10 项错误。
- 已排除路径：仅靠文案/字段存在性检查无法验收交互；改用真实模块行为测试和开发者工具 SDK 点击。
- 最新验证：8 段开发者工具实测通过、0 异常；BPhO 133 份、ESAT 16 份从生产读取；完整细节见 `qa-2026-09-05-runtime-fixes.md`。
- 退出条件：完成本轮源码检查、GitHub 同步和真实截图记录。正式域名、微信换票、iPad 摄像头/麦克风真机另行记录，不宣称已验收。

> 本文件延续 `run-log-2026-09-04.md`；只记录可复核的工程决策和结果，不记录 API key、Cookie、session、私钥、数据库内容或学生个人数据。

## Checkpoint 01 · 四入口壳层、微信登录与计算器

- 当前目标：把首页收敛为四个一级入口（A-Level 学科、IELTS、竞赛/入学考试、Casio 计算器），让入口点击先尝试微信登录，并让 AI Coach 在右上角跨页面可达。
- 下一步依据：截图已复现 `pages/calculator/index` 不存在；同时原首页把内部流程词和登录/AI 入口混在一起。先修真实路由和学生可理解的壳层，再继续迁移 STEM 网页能力。
- 已排除的失败路径：
  - 不直接复制 GitHub 计算器的 `eval` / `Function()` 或远程字体/脚本；已改为本机受限解析器。
  - 不把 Casio 页面伪装成官方模拟器；页面明确为 Stemist 工具。
  - 不把微信登录失败伪装成成功 token；换票失败时进入本机浏览模式，并提供 Account 恢复路径。
  - 不继续用 ActionSheet 代替真实功能页；Practice、Coach、Progress、Notebook、真题和完整网页入口保持独立。
- 最近验证结果：
  - `npm run test:all`：exit 0，覆盖 17 页、WXML/WXSS 编译、相机/裁剪、AI/账号、题库目录、响应式、对比度和安全计算器。
  - STEM candidate `node scripts/test-stem-native-auth.mjs`：exit 0，`/api/auth/wechat` 的共享会话桥接通过。
  - IELTSist `npm run test:wechat-mini-auth`：exit 0，服务端 code2session 映射、重复登录和无效 code 失败闭环通过；`session_key` 未进入响应/持久化。
  - GitHub 公开项目审查记录见 `docs/calculator-integration.md`；候选项目许可证为 MIT，但因动态代码执行未直接导入。
  - 微信开发者工具 CLI 安装检查 exit 0；`wechatide auth -c Codex` 仍返回 `CONNECT_ERROR / wait WechatIDE authorization timeout`，因此没有伪造模拟器截图。
- 退出条件：
  1. DevTools CLI 授权后，刷新项目并完成四入口点击、右上角 AI、手机/iPad 横竖屏真实截图；
  2. 生产环境配置微信 AppID/Secret 后，真实 `wx.login → /api/auth/wechat` 返回短期 STEM 会话；
  3. STEM 完整网页功能（Topic、真题、模拟、进度、Notebook、教师/学校工作区）在安全 WebView/SSO 入口逐项可达；
  4. 完成独立审查、GitHub Actions 和发布前服务器 readiness gate 后才提交/部署。

## Checkpoint 02 · 原生相机、网页能力入口与真题范围

- 当前目标：让 STEM 的“拍照输入”真正进入原生后置相机取景页，并把网页端已有的 Topic、完整真题、模拟考试、进度和 Notebook 能力在小程序中逐项可达；同时避免把多学科题库混在一起。
- 下一步依据：仅调用系统选择器在开发者工具中容易表现成文件选择，不能证明真实相机；只放一个“完整网页”按钮又会让学生看不到功能边界。因此拆出相机页、功能入口网格和按类别的路线选择。
- 已排除的失败路径：
  - 不把 `chooseMedia` 直接当作唯一相机体验；优先 `wx.createCameraContext().takePhoto`，兼容环境才回退到 `sourceType:['camera']`。
  - 不把 `core`、`r1`、`prep` 等来源标签直接展示成筛选条件；统一映射为 IGCSE / Competition / Admissions。
  - 不用固定示例题量；14 个公开目录均通过本地 fixture 审计，学生端只接受服务端 inventory/catalog。
  - 不把 AI、WebView 或登录 token 放进公开 URL；网页接续使用一次性服务端 handoff（STEM 端代码已通过单元验证）。
- 最近验证结果：
  - `npm run test:all`：exit 0，覆盖 18 页（含原生相机和计算器）、WXML/WXSS、页面 handler/link、四入口、分类路由、14 学科目录 fixture、AI/账号/会话恢复。
  - `npm run test:camera-page`：exit 0；真实 CameraContext 路径和 camera-only 兼容路径均有回归。
  - `npm run test:paper-fixtures`：exit 0；每个目录均有 active QP，AS/A2、IGCSE、竞赛和入学筛选都有可映射数据。
  - STEM candidate `node scripts/test-stem-native-auth.mjs`：exit 0；微信换票和 STEM WebView handoff 的过期/单次消费/开放重定向防护通过。
  - IELTSist `npm run test:wechat-mini-auth`：exit 0；服务端只持久化 openid/unionid 映射，不持久化 `session_key`。
- 退出条件：先完成下面的真实 DevTools/真机段，再做 GitHub 推送：
  1. 手机竖屏：四入口逐个点击，A-Level/竞赛进入原生相机，拒绝权限后可从设置回来重试；
  2. iPad 竖/横屏：相机取景、裁剪、AI 右上角按钮和网页功能网格不遮挡；
  3. 真实微信 AppID/Secret 配置后，默认 `wx.login` 取得可用会话并能调用 AI；
  4. 完成 GitHub Actions、独立安全审查和生产 readiness gate。

## Checkpoint 03 · 右上角 AI、账号接续与运行时绑定

- 当前目标：让 AI Coach 在所有页面保持可见且不遮挡内容；点击首页右上角微信登录直接走 `wx.login`，并让 STEM/IELTSist 网页工作区能在不泄露 bearer token 的前提下接续账号。
- 下一步依据：截图中 AI 入口在底部、登录是页面跳转，且有路由缺失错误；学生进入完整网页时如果重新登录，会破坏“一个学习账户”的主线。因此把 AI 入口统一成右上角固定/首页右上角内联按钮，增加一次性 handoff，并保留本机模式作为网络失败回退。
- 已排除的失败路径：
  - 不在 query、日志或小程序存储中放 access token、session_key 或 App Secret；handoff 只存服务端哈希、一次消费、两分钟过期。
  - 不把 ieltsist token 直接发送到 STEM 或反向发送；两端通过已签名的本地服务端桥接。
  - 不用固定端口做测试；IELTSist 微信换票测试动态分配 loopback 端口，避免 Windows `EADDRINUSE`。
  - 不把 `eval` / `Function()` 的公开计算器源码直接导入；本机解析器通过表达式安全测试。
- 最近验证结果：
  - 小程序 `npm run test:all`：exit 0；18 页、原生相机、四入口、右上角 AI、WeChat auth、WebView handoff、14 学科目录和 WXML/WXSS 均通过。
  - STEM candidate `node scripts/test-stem-native-auth.mjs`：exit 0；本地 STEM handoff 的开放重定向、单次消费和跨 IELTSist 内部桥接通过。
  - IELTSist `npm run test:wechat-mini-auth`：exit 0；WeChat code2session 与 IELTSist WebView handoff 通过。
- 退出条件：必须完成 DevTools CLI/真机操作证据；在授权仍不可用时不伪造“已验收”截图。随后只提交意图内文件，推送 GitHub 并等待 Actions 绿灯；服务器变更前重新跑生产 readiness gate。

## Checkpoint 04 · GitHub Casio 按键移植与安全解析回归

- 当前目标：按用户要求从公开 GitHub 项目导入 Casio 风格实现，同时保持微信小程序可审核、可维护，不能把动态代码执行带进学生端。
- 下一步依据：审计了 `Claxer/CASIO-FX-991-ES-PLUS` 的 MIT 许可证和固定提交 `c80addc72aa02fa7bb33104fff25cdc348fa5d05`。上游的布局、SHIFT 第二功能和科学按键分组适合复用；其 `Function()` 执行器、远程字体和 Electron/HTML 运行时不适合小程序。
- 已完成：
  - 导入 `third_party/claxer-casio-fx-991-es-plus/keypad.js` 的声明式按键元数据、MIT `LICENSE` 和 `UPSTREAM.md`，并由计算器页面实际引用；SHIFT 可切换平方/立方、根号/立方根、三角反函数、log/10ˣ、ln/eˣ 等第二功能。
  - 保留 Stemist 自有安全解析器，修正幂运算与一元负号的优先级；输入框回车改走显式确认处理器。
  - 修正等号按钮为整行触控目标，记忆键独立分组，手机/iPad 仍分别保持 88rpx / 48px 以上触控高度。
- 已排除的失败路径：
  - 不复制上游 `eval` / `Function()`、远程 Google Fonts、SVG/HTML 外壳或 Casio 商标素材。
  - 不把第三方项目的未经授权执行逻辑当成“已接入”；只接入可追溯、可审计的 MIT 声明式布局数据。
- 最近验证结果：
  - `npm run test:calculator`：exit 0；上游 revision/许可证、无动态执行、SHIFT 映射、三角/根号/幂运算和回车确认契约均通过。
  - `npm run test:all`：exit 0；18 页、WXML/WXSS 编译、相机、四入口、AI/账号、题库目录、响应式和安全扫描均通过。
  - `git diff --cached --check`：exit 0；`project.config.json` 保持未暂存，未纳入提交。
- 退出条件：完成 GitHub SSH 提交和 Actions 绿灯后，再由微信开发者工具/真机验证四入口、Calculator 的 SHIFT/历史/记忆和手机+iPad 触控；真实微信 AppID/Secret 仍需由部署环境配置，不能在客户端伪造。

## Checkpoint 05 · GitHub 提交与 CI 验证

- 当前目标：让本次 Casio 计算器和统一四入口架构在远端仓库可直接拉取，并保留本地开发者配置。
- 下一步依据：SSH 推送成功，远端 `master` 已指向 `1e5b548c559b86835bc47baf4705c004333a8db1`；GitHub Actions 运行 `33908757888` 的 contracts job 全部成功。
- 已排除的失败路径：不把本机 `project.config.json`（含用户自己的开发者配置）提交到公开仓库；不把 DevTools 未授权时的模拟器画面当成真实验收证据。
- 最近验证结果：本地 `npm run test:all` exit 0；远端 `npm run test:all` 和微信编译契约均 exit 0；工作树只剩用户本地的 `project.config.json` 未提交修改。
- 退出条件：在微信开发者工具重新编译后，按手机竖屏和 iPad 竖/横屏分别实测四入口、SHIFT 第二功能、历史/记忆、AI Coach 和相机链路；如发现真实设备差异，再以新的独立修复提交处理。

## Checkpoint 06 · STEM 数据范围与 IELTSist 全功能目录

- 当前目标：让 A‑Level 与竞赛/入学考试复用同一套前端交互但不混数据，并把 IELTSist 生产端全部一级能力在小程序中可发现、可深链进入。
- 下一步依据：生产 `ieltsist.com` 当前包含 Dashboard、Single Module、Writing、Speaking bank、Same‑Test、Random Exam、Vocabulary、Mine、Subscription 和全局 AI Coach；原小程序只列四项技能，且 STEM 拍题/进度没有保存 category/family。
- 已完成：
  - 新增 `utils/ieltsCatalog.js`，12 个功能映射为 5 个学生可理解的分组；原生 Listening/Reading/Writing/Speaking/Coach 快速页保留，完整音频、PDF、计时、报告、词汇、账号和会员通过安全 WebView 深链承载。
  - IELTS 原生 Coach 改走 allowlisted `https://ieltsist.com/api/help/chat`；不把 STEM bearer token 冒充 IELTSist 浏览器会话，正式历史和会员状态仍由一次性 WebView handoff 持久化。
  - STEM `category/family` 从入口贯穿路线选择、拍题上下文、attempt 摘要、Progress 回放、Past papers 过滤和 WebView AI Coach 上下文；Admissions 自动使用 `family=admissions`。
  - Notebook 也按入口过滤路线，并修复“默认竞赛路线先于显式 ESAT/TMUA 路线命中”的深链错误；现在先精确恢复请求路线，再使用默认值。
  - iPad 横屏使用 3/4 列 IELTS 功能卡，手机使用 2 列且保留 88rpx 触控高度；真题目录按入口只显示对应阶段过滤器。
  - 移除 Practice 页重复的三段产品筛选条，改为单一“当前工作区 / 切换入口”栏；顶部/底部 Practice 导航会保留当前 IELTS/A-Level/竞赛范围。
- 已排除的失败路径：
  - 不把 IELTS 四项技能的简化文本页误称为完整 IELTSist；每个快捷页均提供完整工作区入口。
  - 不通过客户端 query 自证权限；category/family 只是范围提示，正式题目、分数、账号和数据仍由各自服务端校验。
  - 不让 WebView 中的 AI FAB 在竞争题或 IELTS 页面丢失产品上下文。
- 最近验证结果：
  - `npm run test:all`：exit 0（含 IELTS 生产视图映射、纸张入口范围、WebView 上下文、API origin 和微信编译）。
  - `node --check`：44 个小程序 JS 文件全部通过；范围化 secret scan 无命中。
  - UI Pro Max UX 检索确认移动端触点 ≥44px、相邻间距 ≥8px、移动优先布局；当前 CSS 契约已覆盖手机/iPad 功能卡和入口范围栏。
  - 生产只读 API：A‑Level 9702 AS 为 `ready=true`；BPhO、AMC 12、ESAT、TMUA 当前均为 `verified=0 / available=0`，小程序保持诚实的不可正式练习状态。
  - IELTSist `#home/#sequence/#exam/#vocabulary/#mine/#subscription` 深链均 HTTP 200；`/api/help/chat` 空输入按契约返回 400。
- 退出条件：微信开发者工具重新拉取并编译后，逐项点击 IELTS 12 个入口和 STEM 两个入口，验证真实 WebView 业务域名、微信登录 handoff、相机和低网失败状态；未完成真机前不宣称发布就绪。

## Checkpoint 07 · 远端交付与剩余真机门禁

- 当前目标：把范围拆分和 IELTSist 功能目录交付到可拉取的 GitHub `master`，并验证远端 CI 与本地结果一致。
- 下一步依据：提交 `478522f92bb5d69a9e5fba2bb366bb1a0528f5db` 已通过 SSH 推送；GitHub Actions `33918857082` 已完成。
- 已排除的失败路径：未提交用户本地 `project.config.json`；未把 Actions 绿色当作相机、麦克风、WebView 业务域名或真实微信会话已经通过。
- 最近验证结果：GitHub `Mini Program contracts` 的 `npm run test:all` 和微信编译检查均 success；工作树只剩用户本地 AppID 配置修改。
- 退出条件：在微信开发者工具拉取 `master`、点“编译”，再用手机/iPad 验证全部入口；竞赛正式 Topic 练习仍受生产 `verified/available=0` 数据门阻塞，拍题与目录浏览可用但不能伪装成正式题库。
